<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Debug builds only. exported=true so adb can reach it; that is
          acceptable precisely because this file is not merged into release.
        -->
        <receiver
            android:name="dev.dheirav.thirsttrap.debug.DebugReceiver"
            android:exported="true" />
    </application>
</manifest>
